# The specification's base document

`Product_Specification_ImB.html` is the owner's own blank template — the approved design
for the intermediate-bulk product specification, fetched from Drive on 21.09.2026
(`templates/product-spec-imb/`, file id `1RI4r5AnUSxtxRglHgXgXFXWvqAsQIdYQ`).

It is here to be filled, never edited. The same rule the certificates follow: data is
applied onto a settled visual layer, and the layer itself is not touched.

## Why it is the authority

The Drive folder of 57 per-grade sheets in the *PROD_SPEC* design carries a
`_SUPERSEDED.md` dated 20.09.2026:

> SUPERSEDED — do not issue from this folder. Canonical product-specification design
> `Final_Docs/ImB_SPC/SPC_FINAL_ImB`. Design-system template
> `templates/product-spec-imb/Product_Specification_ImB.html`. … Nothing here may be
> issued, copied as a starting point, or cited as the current specification.

The desk's own `specs/QCSP_001_v04/SHEETS` were built on neither of those. They are wrong
in the same way the superseded folder is wrong, and they are to be rebuilt on this file.

## What may be filled, and nothing else

    <title>                      the document's name
    .hb-code                     QCSP 001_<CULT>-<GRADE>_v.01
    .pb-name                     the cultivar
    .pbp-val / .pbp-tol          the nominal and its tolerance
    .pb-type-pill × 3            Phenotype · Chemotype · Processing — ☐ &#9744; becomes ☒ &#9746;
                                 the Phenotype pill's INDICA nn : SATIVA nn only where the
                                 record states explicit numbers; BALANCED is not 50:50
    .pcr-val × 3                 Product Code · Potency · Spec. doc. code
    .ap-date-val × 2             the two approval dates

**Section 02 is not data.** Its twenty-eight rows and four columns — № · Parameter ·
Method / Reference · Acceptance Criteria, with families 9, 10, 11 and 12 grouped — are the
template's own and are carried through untouched. #4 states *Per target grade as per
Section 01*; it does not repeat the window.

**The footer carries no document code** (owner, 21.09.2026), and the specification is
**QCSP 001 v.03** — the desk's v.04 bump is withdrawn.

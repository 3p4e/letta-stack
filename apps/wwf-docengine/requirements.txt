# GrowFlow DocEngine — pinned service deps (engine deps: python-docx + lxml).
# Kept in lockstep with backend/requirements.txt for the shared web stack:
# this service was previously a year behind on fastapi/starlette and had never
# been scanned by CI at all, so it still carried the 0.41.x starlette
# advisories the backend had already moved off.
fastapi==0.139.0
# starlette pinned explicitly (normally a fastapi-transitive) so CI's
# pip-audit sees the exact deployed version — 0.41.x carried 8 advisories.
starlette==1.3.1
uvicorn[standard]==0.34.0
httpx==0.28.1
asyncpg==0.30.0
pydantic==2.10.4
python-docx==1.2.0
# lxml 5.3.0 carried PYSEC-2026-87; 6.1.0 is the fixed line.
lxml==6.1.0
PyYAML==6.0.2
